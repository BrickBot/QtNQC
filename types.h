#ifndef _TYPES_H_
#define _TYPES_H_

enum types_enum
{
  GATE = 1,
  LOOP,
  ADD_GATE,
  FOR_LOOP,
  WHILE_LOOP,
  DO_WHILE_LOOP,
  DIVIDE_GATE,
  MULTIPLY_GATE,
  SUBTRACT_GATE,
  NOT_GATE,
  AND_GATE,
  OR_GATE,
  EQUALS_GATE,
  GREATER_THAN_GATE,
  LESS_THAN_GATE,
  GREATER_THAN_OR_EQUAL_TO_GATE,
  LESS_THAN_OR_EQUAL_TO_GATE,
  REPEAT_LOOP,
  NOT_EQUAL_TO_GATE,
  BITWISE_AND_GATE,
  BITWISE_XOR_GATE,
  BITWISE_OR_GATE,
  UNARY_MINUS_GATE,
  BITWISE_NEGATION_GATE,
  MODULO_GATE,
  RIGHT_SHIFT_GATE,
  LEFT_SHIFT_GATE,
  INCREMENT_GATE,
  DECREMENT_GATE,
  SIGN_OF_GATE,
  ABSOLUTE_GATE,
  LESS_THAN_ZERO_GATE,
  GREATER_THAN_ZERO_GATE,
  GREATER_THAN_ONE_GATE,
  CONSTANT,
  VARIABLE,
  TASK,
  FUNCTION,
  WIRE,
  CURSOR,
  DELETE,
  DELETEALL,
  IF,
  SWITCH,
  SUBROUTINE,
  BREAK,
  CONTINUE,
  RETURN
};

#endif
